{
  "name": "Library",
  "version": "1.0.0",
  "dependencies": {
    "@sap/cds": "^9.9.1"
  },
  "devDependencies": {
    "@cap-js/sqlite": "^2.4"
  },
  "scripts": {
    "start": "cds-serve"
  },
  "cds": {
    "requires": {
      "db": {
        "kind": "sqlite",
        "model": "db",
        "credentials": {
          "database": "library.db"
        }
      }
    }
  },
  "private": true
}
